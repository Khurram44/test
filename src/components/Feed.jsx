import React from "react"
import { Box } from "@mui/material";

const Feed = ({ children }) => {
    return (
         <Box className="feed" sx={{backgroundColor:"green"}}>
           Feed
        </Box>
    )
}

export default Feed;