import { Box } from "@mui/material";
import React from "react"

const SideBar = ({ children }) => {
    return (
        <Box className="SideBar" sx={{backgroundColor:"red"}}>
           SideBar
        </Box>
    )
}

export default SideBar;