import React from "react"
import { Box } from "@mui/material";

const NavBar = ({ children }) => {
    return (
        <Box className="RightBar" sx={{backgroundColor:"yellow"}}>
        NavBar
        </Box>
    )
}

export default NavBar