import React from "react"
import { Box } from "@mui/material";

const RightBar = ({ children }) => {
    return (
        <Box className="RightBar" sx={{backgroundColor:"green"}}>
            RightBar
            </Box>
    )
}

export default RightBar